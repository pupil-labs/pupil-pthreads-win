os: windows
language: sh

install: choco install python --version 3.6.8

deploy:
  on:
    branch: master
    # tags: true
  provider: pypi
  user: pupil-labs
  password : $PYPI_APITOKEN
  distributions: "bdist_wheel"
